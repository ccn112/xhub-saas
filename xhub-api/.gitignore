# compiled output
/dist
/node_modules
/build

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / IDE
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json

# Tests
/coverage
/.nyc_output

# Local env (secrets) — keep .env.example committed
.env
.env.*
!.env.example

# Prisma local db (if using SQLite for quick dev)
*.sqlite
*.sqlite-journal

# Geo ingestion Python virtualenv (scripts/geo-hapulico-ingest.mjs's Overture
# path) — local only, recreate with: python3 -m venv .venv-geo && .venv-geo/bin/pip install duckdb
.venv-geo

# Local runtime data (per-tenant backups, DATA-03 supplier media cache) —
# never committed, regenerated by the relevant seed/ingest scripts.
/storage
